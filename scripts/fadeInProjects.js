document.addEventListener("DOMContentLoaded", function () {
  setTimeout(function () {
    const fadeDiv = document.getElementById("projects-main-wrap");
    fadeDiv.style.opacity = 1;
  }, 2000); // Adjust the delay (in milliseconds) as needed
});
